import React from 'react';

const Casilla = ({ row, col }) => {


  return (
    <button
      style={{
        width: '40px',
        height: '40px',
        margin: '2px',
        fontSize: '16px',
        cursor: 'pointer'
      }}
    >
    </button>
  );
};

export default Casilla;
